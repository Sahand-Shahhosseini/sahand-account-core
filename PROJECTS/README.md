# PROJECTS

Each project lives in its own folder.
Rule: projects may override **content** (docs/data), but should NOT override Project-L law unless explicitly requested via tokens.

Suggested structure per project:
- `SPEC/`
- `DOCS/`
- `DATA/`
- `RELEASES/` (optional pointers)
